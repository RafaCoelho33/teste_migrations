table:
  name: calculator_methods_calculated
  schema: public
object_relationships:
  - name: calculator_lead
    using:
      foreign_key_constraint_on: lead
  - name: calculator_methods_calculated_type
    using:
      foreign_key_constraint_on: type
insert_permissions:
  - role: anonymous
    permission:
      check: {}
      columns:
        - id
        - lead
        - type
        - value
        - created_at
        - updated_at
